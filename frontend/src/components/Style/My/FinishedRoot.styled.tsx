import { colors } from '@/styles/colorPalette';
import styled from 'styled-components';

export const FinishedRootContainer = styled.div`
  .card-list {
    margin-top: 24px;
    display: flex;
    flex-direction: column;
    gap: 30px;
    padding: 0 16px;
  }
`;
